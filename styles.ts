import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    justifyContent: "center",
    gap: 16,
    padding: 32,
  },
  input: {
    borderWidth: 1,
    borderColor: "#333",
    marginTop: 10,
    borderRadius: 5,
    height: 20,
    padding: 16,
  },
});
